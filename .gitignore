# Raw source material — huge, already extracted/processed into site/public.
# Keep these files locally, just don't version them.
drive-download-*.zip
files.zip
каталог/

# Claude Code local session state
.claude/

# Reference skill packages — local tooling reference, not site content
Skill/
